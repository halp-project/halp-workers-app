export class Product {
  id: number;
  title: string;
  description: string;
  image: string;
  price: number;
  quantity: number;

  constructor(
    id: number,
    title: string,
    description: string,
    image: string,
    price: number,
    quantity: number
  ) { }
}
